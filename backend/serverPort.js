//import app from "./index"

//app.listen(3003, () => "Listening on port 8080");

// const server = app.listen(PORT, () =>
//   console.log(`CalisthenicsHealth server is running on port ${PORT}`)
// );

